from game import Game
from main.src.clippy import Clippy
from main.src.gamestate import Gamestate


class TestGame:
    def test_get_opposing_player(self):
        # TODO create mock of Reporter class and finish test

        # gamestate = Gamestate(Clippy("current player"), Clippy("opposing player"))
        # Game(reporter)
        # opposing_player = get_opposing_player(current_player, state)
        assert 1 == 1